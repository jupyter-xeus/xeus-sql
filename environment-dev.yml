name: xeus-sql
channels:
  - conda-forge
dependencies:
  # Build dependencies
  - cmake
  - cxx-compiler
  - ninja
  # host dependencies
  - cpp-tabulate>=1.5
  - cppzmq
  - nlohmann_json
  - soci-core
  - xeus>=5.0,<=6
  - xeus-zmq
  - xproperty=0.11.0
  - xvega-bindings=0.0.10
  - xvega=0.0.10
  # Test dependencies
  - doctest >= 2.4.6
