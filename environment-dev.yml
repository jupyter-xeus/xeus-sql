name: xeus-sql
channels:
  - conda-forge
dependencies:
  # Build dependencies
  - cmake <4
  - cxx-compiler
  - ninja
  # host dependencies
  - cpp-tabulate>=1.5
  - cppzmq
  - nlohmann_json
  - soci-core
  - xeus-zmq>=4,<5
  - xproperty
  - xvega-bindings>=0.1.1
  - xvega>=0.1.3
  # Test dependencies
  - doctest >= 2.4.6
