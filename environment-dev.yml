name: xeus-sql
channels:
  - conda-forge
dependencies:
  # Build dependencies
  - cmake
  - cxx-compiler
  - ninja
  # host dependencies
  - cpp-tabulate>=1.5
  - cppzmq
  - nlohmann_json
  - soci-core
  - xeus-zmq>=3,<4
  - xproperty
  - xvega-bindings>=0.1.0
  - xvega>=0.1.0
  # Test dependencies
  - doctest >= 2.4.6
