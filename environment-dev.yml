name: xeus-sql
channels:
  - conda-forge
dependencies:
  # Build dependencies
  - cmake
  - cxx-compiler
  - ninja
  # host dependencies
  - cpp-tabulate>=1.5
  - cppzmq
  - nlohmann_json
  - soci-core
  - xeus-zmq>=3.1.1,<4
  - xproperty
  - xvega-bindings>=0.1.1
  - xvega>=0.1.3
  # Test dependencies
  - doctest >= 2.4.6
